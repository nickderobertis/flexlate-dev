commands: []
data: {}
run_configs:
  base:
    post_update:
    - touch something_else.txt
    pre_update:
    - touch something.txt
  default_publish:
    post_init:
    - gh repo create --source=.
    post_update:
    - git push --all origin
  default_serve: {}
  my-run-config:
    auto_commit_message: something
    extends: base
    pre_update:
    - touch overridden.txt
