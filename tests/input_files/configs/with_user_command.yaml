commands:
- id: separate_command
  run: touch referenced.txt
data: {}
run_configs:
  default_publish:
    post_init:
    - gh repo create --source=.
    post_update:
    - git push --all origin
  default_serve: {}
  my-run-config:
    post_init:
    - run: touch user_command.txt
    - id: separate_command
    post_update:
    - touch string_command.txt
