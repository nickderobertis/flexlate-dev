commands: []
data: {}
run_configs:
  default:
    publish:
      post_init:
      - gh repo create --public --source=.
      - git push --all origin
      post_update:
      - fxt merge
      - git push --all origin
  my-run-config:
    post_init:
    - background: true
      run: sleep 10
